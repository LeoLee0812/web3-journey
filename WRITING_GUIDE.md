# 写作规范（所有小节作者 / 章节审校必读）

本项目是 **Astro + Starlight** 静态知识库《从 0 开始的 Web3 之旅》，读者是**中文母语、Web3 知识几乎为 0** 的成年人（求职方向：Web3 运营 / 研究分析 / AI 自动化）。

## 一、文件与路径

- 每个小节一个文件：`src/content/docs/<章节目录>/<小节文件名>.mdx`
- 目录、文件名严格按 `outline.json` 里的 `id`，**不要自创路径**。
- 必须是 `.mdx`（不是 `.md`）。

## 二、Frontmatter 模板（必须一字不差地遵守结构）

```mdx
---
title: 小节标题（照抄 outline.json 的 title，不要改写）
description: 一句话概括本节讲什么，60-90 字，会用于 SEO 与搜索结果。
sidebar:
  order: 小节序号（1-8，整数）
---
```

⚠️ frontmatter 里 **不要**写 `head`、`banner`、`tableOfContents` 等其它字段。标题里若含英文缩写，直接写即可，不要加引号包裹整行（若 title 含冒号 `:`，必须用双引号把整个值包起来）。

## 三、正文结构（每节固定骨架）

```mdx
import { Aside, Steps, Card, CardGrid, Tabs, TabItem } from '@astrojs/starlight/components';
import Mermaid from '@components/Mermaid.astro';

> **一句话结论：** ……（30-60 字，先给答案）

## 为什么要讲这个 / 从一个你熟悉的场景说起
（用日常类比开场，200-400 字，禁止上来就堆术语）

## 核心机制拆解
（本节主体，2-4 个二级标题，配 Mermaid 图或表格）

## 🧪 动手环节 / 实战流程
（可跟做的步骤，用 <Steps> 组件。没有实操的理论节可换成「真实案例」）

## ⚠️ 常见坑与误区
（3-5 条，尽量来自真实事故或常见新手错误）

## 本节要点
（4-6 条 bullet，每条一行，读者只看这里也能复习）

## 延伸阅读
（2-4 条外部权威链接，markdown 链接格式，必须是真实存在的官方文档/白皮书/知名研究站）
```

## 四、篇幅与密度

- **每节正文 1000–1400 个中文字**（不含 frontmatter 和代码块）。宁可写满，不要凑字数。
- 每节至少包含 **1 个表格** 或 **1 个 Mermaid 图**。
- 每节至少 **1 个具体数字/参数/公式**（例如 Gas 单位、区块时间、抵押率算法），并注明口径与大致时间。
- 严禁空话套话：「随着行业的发展」「具有重要意义」这类句子一律删掉。

## 五、可用组件（Starlight 官方 + 自定义）

| 组件 | 用途 | 示例 |
| --- | --- | --- |
| `<Aside type="note|tip|caution|danger" title="…">` | 提示 / 警告框 | 风险提示统一用 `caution` 或 `danger` |
| `<Steps>` 包裹有序列表 | 实操步骤 | 内部必须是 `1. 2. 3.` 的有序列表 |
| `<CardGrid>` + `<Card title icon>` | 并列概念 | icon 用 `rocket` `setting` `star` `warning` `information` |
| `<Tabs>` + `<TabItem label="…">` | 多方案对比 | |
| `<Mermaid chart={\`graph LR; A-->B;\`} />` | 流程图 | **必须用反引号模板字符串** |

### Mermaid 注意事项（很容易写错，务必遵守）

- 语法：`<Mermaid chart={\`graph TD\n  A["中文节点"] --> B["另一个"]\n\`} />`
- **节点文字里含中文、括号、冒号时，必须用双引号包起来**：`A["以太坊(ETH)"]`。
- 不要用 `graph LR;` 后面接分号连写多行，用换行。
- 只用这几种最稳的图型：`graph TD` / `graph LR` / `flowchart TD` / `sequenceDiagram`。
- 图不要太大，节点控制在 10 个以内。

### 图片引用

配图由专门的配图 Agent 生成，放在 `public/images/<章节目录>/`。正文里这样引用：

```mdx
![描述性 alt 文本](/images/04-wallets/hero.jpg)

*图注：一句话说明这张图在讲什么*
```

作者写作时**不要**自己插图片路径（除非被明确告知文件名已存在），配图由配图 Agent 统一插入。

## 六、内容准确性红线

1. **时效**：加密行业变化极快。凡是涉及「当前主流」「最新版本」「多少 TVL」「谁排第一」的表述，必须依据 `research/` 目录下对应章节的调研简报，并在文中标注时间口径（如「截至 2026 年中」）。
2. **不编造**：不确定的数字、项目名、事件时间，宁可不写，或写成区间/定性描述。绝不虚构外部链接。
3. **不荐币**：任何项目只作为机制讲解的例子，禁止出现「值得买入」「必涨」「推荐关注」等表述。
4. **不给绝对承诺**：收益率、空投预期一律写清「历史数据不代表未来」。
5. **合规**：涉及交易所、税务、KYC 的内容要提醒读者「各地法规差异大，自行核实」。

## 七、语言风格

- 简体中文，句子短，多用第二人称「你」。
- 中英混排时英文两侧留空格：`使用 MetaMask 钱包`。
- 术语首次出现给中英对照：`无常损失（Impermanent Loss，IL）`。
- 禁止 AI 腔：不要「让我们一起」「总而言之」「在当今这个时代」「不仅……而且……」的排比堆砌。
- 允许有观点、有判断，但要给理由。

## 八、交付要求

- 直接把文件写到指定路径，**写完即完成**，不要把全文当作返回值输出。
- 返回值只需一行 JSON：`{"file":"路径","words":正文中文字数,"notes":"需要上级注意的问题"}`
